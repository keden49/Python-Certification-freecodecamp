## Project Overview
This project contains both the original and refined number pattern generator which takes an integer 'n' and returns a string with incrementing values from 1 to 'n'. With each number spaced from the other.

